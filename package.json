{
  "name": "team-everything",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "start": "node api/index.js",
    "build": "rm -rf api/public && cd api && npm install && cd ../frontend && npm install && npm run build && mv ./build ../api/public && cd ..",
    "clean": "rm -rf api/node_modules api/public frontend/node_modules",
    "rebuild": "npm run clean && npm run build",
    "oops": "npm run rebuild && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "-": "^0.0.1",
    "bcrypt": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "esm": "^3.2.25",
    "express": "^4.17.1",
    "express-pino-logger": "^5.0.0",
    "express-validator": "^6.8.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.11.6",
    "multer": "^1.4.2",
    "pino": "^6.7.0",
    "pino-pretty": "^4.3.0",
    "save": "^2.4.0",
    "uuid": "^8.3.2"
  }
}
