let mongoose;_0bc‍.x([["PeopleModel",()=>PeopleModel]]);_0bc‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const PersonSchema = mongoose.Schema(
  {
    name: String,
    clothes: String,
    height: Number,
    employed: Boolean,
    children: [{ name: String, age: Number }],
  },
  {
    timestamps: true,
  }
);

       const PeopleModel = mongoose.model("Person", PersonSchema);
